# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# KMS Configuration (will be created during deployment)
KMS_KEY_ID=arn:aws:kms:REGION:ACCOUNT_ID:key/KEY_ID

# Secrets Manager Configuration (will be created during deployment)
SECRETS_ARN=arn:aws:secretsmanager:REGION:ACCOUNT_ID:secret:SECRET_NAME