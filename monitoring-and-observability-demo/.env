# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# Serverless Configuration
STAGE=dev
SERVICE_NAME=aws-monitoring-workshop

# Optional: Custom table name (if not using default)
# DYNAMODB_TABLE=custom-table-name